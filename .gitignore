# Terraform
**/.terraform/
**/.terraform.tfstate
**/.terraform.tfstate.backup
*.tfstate
*.tfstate.*
*.tfplan
*.plan
tf.plan
crash.log
crash.*.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
.terraformrc
terraform.rc

# Local var files (commit terraform.tfvars.example only)
terraform.tfvars
*.auto.tfvars
*.auto.tfvars.json

# Account-specific backend bucket name (generated by bootstrap-state-bucket.sh)
backend.hcl

# Local AWS / kube credentials — must never be committed
.aws/
*.kubeconfig
kubeconfig
.kube/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Editor swap files
*.swp
*.swo
*~

# Local logs and scratch
*.log
.scratch/

# Claude Code local config
.claude/
